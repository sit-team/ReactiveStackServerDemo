# ReactiveStackServerDemo

A fully reactive stack project with embedded MongoDB and Reactor Core

## Usage

Requires java 17.

```shell
./mvnw clean install
java -jar target/ReactiveStackServerDemo-0.0.1.jar
```
